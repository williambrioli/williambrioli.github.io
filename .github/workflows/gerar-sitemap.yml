name: Gerar e atualizar sitemap.xml automaticamente

on:
  schedule:
    - cron: "0 6 * * *"   # Executa todos os dias às 6h UTC (3h da manhã no Brasil)
  workflow_dispatch:

permissions:
  contents: write   # ✅ Permite que o Actions faça commit e push

jobs:
  gerar-sitemap:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Clonar repositório
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Instalar dependências
        run: |
          npm init -y
          npm install node-fetch@2

      - name: 🧩 Gerar sitemap automático
        run: node gerar-sitemap.js

      - name: 💾 Fazer commit e push do novo sitemap.xml
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add sitemap.xml
          git commit -m "🤖 Sitemap atualizado automaticamente em $(date '+%Y-%m-%d %H:%M')" || echo "Nenhuma alteração"
          git push
